---
title: 'requirements'
description: The `requirements` key defines your dependencies
---

## Overview
  ```yaml alto.yml
  ...
  requirements: ./requirements.txt
  ...
  ```

  The requirements key should point to the `.txt` file containing your code's
  dependencies. This is an **optional** field.